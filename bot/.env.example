# Скопируй в .env и заполни своими данными
# .env добавить в .gitignore — никогда не коммить реальные ключи!

# Получить на: https://open.ctrader.com → My Apps → New Application
CTRADER_CLIENT_ID=your_client_id_here
CTRADER_CLIENT_SECRET=your_client_secret_here

# OAuth токены для торгового аккаунта
# Получить через OAuth flow: https://connect.spotware.com/apps/auth
# REFRESH_TOKEN опционален (оставь пустым, если нет) — будет использован
# будущей реализацией авто-рефреша access_token-а при истечении.
CTRADER_ACCESS_TOKEN=your_access_token_here
CTRADER_REFRESH_TOKEN=your_refresh_token_here

# ID торгового аккаунта (число, видно в cTrader платформе)
CTRADER_ACCOUNT_ID=12345678

# demo.ctraderapi.com — для тестирования
# live.ctraderapi.com  — для реальных денег (НЕ ТОРОПИСЬ!)
CTRADER_HOST=demo.ctraderapi.com
CTRADER_PORT=5035

# Список символов (через запятую) на spot-подписку при старте.
# Имена, не ID — разрешаются через каталог (ProtoOASymbolsListRes).
# Пусто = без живых тиков (REST всё равно работает, но /api/quotes пуст).
# Пример: EURUSD,ETHEREUM,GBPJPY
CTRADER_SUBSCRIBE_SYMBOLS=EURUSD,ETHEREUM

# Уровень логирования: trace / debug / info / warn / error
RUST_LOG=ctrader_bot=debug

# Формат логов: `json` — построчный JSON (для Loki/Promtail/Vector),
# любое другое значение или пусто — человекочитаемый вывод для dev.
# LOG_FORMAT=json
